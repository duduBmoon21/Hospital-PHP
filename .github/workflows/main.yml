on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ftp.ducodeverse.com
        username: ${{ secrets.FTP_USERNAME01 }}
        password: ${{ secrets.FTP_PASSWORD01 }}
        local-dir: ./dist/
